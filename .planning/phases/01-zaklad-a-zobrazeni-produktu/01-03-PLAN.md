---
phase: 01-zaklad-a-zobrazeni-produktu
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - DISP-05
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - LEGAL-01
  - LEGAL-02

must_haves:
  truths:
    - "Navstevnik vidi hero sekci s nazvem znacky, podtitulkem a poetickym textem"
    - "Navstevnik vidi vsechny 4 produktove karty s fotkami, popisy a cenami"
    - "Navstevnik vidi sekci Pribeh s textem o Misce"
    - "Navstevnik vidi sekci hodnot (cesky len, strojova vysivka, krasne baleni)"
    - "Navstevnik vidi banner akce 3+1"
    - "Fixni navigace funguje a hamburger se otevira na mobilu"
    - "Hero fade-in animace je videt pri nacteni stranky"
    - "Stranka je plne responzivni na mobilu (390px)"
    - "Patticka obsahuje oba legalni texty (14 dni vraceni, GDPR)"
  artifacts:
    - path: "index.html"
      provides: "HTML kostra overena vizualne"
    - path: "css/style.css"
      provides: "Kompletni styling overeny vizualne"
    - path: "js/products.js"
      provides: "PRODUCTS data overeny — karty se zobrazuji"
    - path: "js/main.js"
      provides: "Nav a render logika overena funkcne"
    - path: ".nojekyll"
      provides: "Pritomen v koreni"
  key_links:
    - from: "Navstevnik"
      to: "Vsechny sekce viditelne"
      via: "Vizualni overeni v prohlizeci"
      pattern: "manual"
---

<objective>
Vizualni a funkcni overeni kompletni Phase 1.

Purpose: Automatizovane testy proverily strukturu souboru a JS logiku, ale vizualni stranku — barvy, fonty, layout, responzivitu, animace — muze overit pouze lidske oko. Tento plan je checkpoint, kde Misa potvrzuje ze stranka vypada jak ma pred zahajenim Phase 2.

Output: Potvrzena vizualne kompletni stranka pripravena pro Phase 2.
</objective>

<execution_context>
@C:/Users/misah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/misah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-zaklad-a-zobrazeni-produktu/01-01-SUMMARY.md
@.planning/phases/01-zaklad-a-zobrazeni-produktu/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalni automatizovana kontrola vsech souboru Phase 1</name>
  <files></files>
  <action>
Spustit finalni kontrolu ze vsechny soubory Phase 1 existuji a maji spravny obsah:

```bash
node -e "
const fs = require('fs');
const path = require('path');
const root = 'C:/Users/misah/Documents/Claude_code/web_misiksvet';

const files = [
  'index.html',
  'css/style.css',
  'js/products.js',
  'js/main.js',
  '.nojekyll'
];

console.log('=== Phase 1 Final File Check ===');
files.forEach(f => {
  const exists = fs.existsSync(path.join(root, f));
  const size = exists ? fs.statSync(path.join(root, f)).size : 0;
  console.log((exists ? 'OK' : 'MISSING') + ' ' + f + (exists ? ' (' + size + ' bytes)' : ''));
});

// Deep checks
const html = fs.readFileSync(path.join(root, 'index.html'), 'utf8');
const css = fs.readFileSync(path.join(root, 'css/style.css'), 'utf8');

eval(fs.readFileSync(path.join(root, 'js/products.js'), 'utf8'));

const deepChecks = [
  ['HTML: lang=cs', html.includes('lang=\"cs\"')],
  ['HTML: charset UTF-8 first', html.indexOf('charset=\"UTF-8\"') < html.indexOf('<title')],
  ['HTML: Cormorant Garamond font link', html.includes('Cormorant+Garamond')],
  ['HTML: Josefin Sans font link', html.includes('Josefin+Sans')],
  ['HTML: hero section', html.includes('id=\"hero\"')],
  ['HTML: pribeh section', html.includes('id=\"pribeh\"')],
  ['HTML: produkty section + product-grid', html.includes('id=\"product-grid\"')],
  ['HTML: hodnoty section', html.includes('id=\"hodnoty\"')],
  ['HTML: akce section', html.includes('id=\"akce\"')],
  ['HTML: kontakt section', html.includes('id=\"kontakt\"')],
  ['HTML: footer', html.includes('<footer>')],
  ['HTML: hero img no lazy', !html.match(/20251208_190557[^>]*loading=\"lazy\"/)],
  ['HTML: LEGAL-01 14 dni', html.includes('14') && html.includes('vrát')],
  ['HTML: LEGAL-02 GDPR osobni udaje', html.includes('osobn')],
  ['HTML: .nojekyll is empty', fs.statSync(path.join(root, '.nojekyll')).size === 0],
  ['CSS: --color-bg token', css.includes('--color-bg')],
  ['CSS: --color-brown token', css.includes('--color-brown')],
  ['CSS: fadeIn keyframes', css.includes('@keyframes fadeIn')],
  ['CSS: sticky nav', css.includes('sticky')],
  ['CSS: mobile hamburger hidden by default', css.includes('.hamburger')],
  ['CSS: .nav-links.open', css.includes('.nav-links.open')],
  ['JS: PRODUCTS array has 4 items', PRODUCTS.length === 4],
  ['JS: zastera price 890', PRODUCTS.find(p => p.id === 'zastera').price === 890],
  ['JS: relative image paths', PRODUCTS.every(p => !p.image.startsWith('/'))]
];

console.log('');
console.log('=== Deep Content Checks ===');
deepChecks.forEach(([name, ok]) => console.log((ok ? 'OK' : 'FAIL') + ' ' + name));

const failed = deepChecks.filter(([, ok]) => !ok);
if (failed.length > 0) {
  console.log('');
  console.log('FAILED: ' + failed.length + ' checks failed — opravit pred checkpointem');
  process.exit(1);
} else {
  console.log('');
  console.log('All checks passed. Pripraveno pro vizualni overeni.');
}
"
```

Pokud jake kontroly selzou, opravit prislusne soubory pred pokracovanim na checkpoint.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const checks=['index.html','css/style.css','js/products.js','js/main.js','.nojekyll'].map(f=>[f,fs.existsSync('C:/Users/misah/Documents/Claude_code/web_misiksvet/'+f)]); checks.forEach(([n,ok])=>console.log((ok?'OK':'MISSING')+' '+n)); process.exit(checks.every(([,ok])=>ok)?0:1)"</automated>
  </verify>
  <done>Vsech 5 souboru Phase 1 existuje a prochazi deep content checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vizualni overeni kompletni stranky</name>
  <action>Prezentovat uzivatelce vizualni kontrolni seznam a cekat na potvrzeni. Vsechny predchozi tasky byly automatizovane — tento checkpoint zajistuje ze stranka vypada spravne v prohlizeci pred zahajenim Phase 2.</action>
  <what-built>
Kompletni vizualni stranky Phase 1:
- index.html se vsemi sekcemi (hero, pribeh, produkty, hodnoty, akce, kontakt, paticka)
- css/style.css s design system tokeny, mobile-first stylingem a responzivnim layoutem
- js/products.js s PRODUCTS polem (4 produkty)
- js/main.js s renderovanim karet a inicializaci navigace
- .nojekyll v koreni
  </what-built>
  <how-to-verify>
**Otevrit stranku:**
Otevrit `C:/Users/misah/Documents/Claude_code/web_misiksvet/index.html` v prohlizeci (dvojklik nebo Live Server).

**Vizualni kontroly — desktop (siroka obrazovka):**

1. Navigace: Logo "Mišik svět" vlevo, tři odkazy (Příběh · Výrobky · Kontakt) vpravo, fixni při scrollu
2. Hero sekce: velký nadpis "Mišik svět", podtitulek "Ručně vyšívané lněné výrobky", poetický text; fotka na pozadí; fade-in animace viditelná při prvním načtení (obnovit stranku F5 pro zopakování)
3. Sekce Příběh: text o Míše (3 odstavce), foto produktu vedle textu (dvousloupec)
4. Sekce Výrobky: 4 karty v mřížce — Kopretina, Vlčí mák, Šiška, Zástěra — každá s fotkou, popisem, materiálem a cenou; 3 utěrky za 380 Kč, zástěra za 890 Kč
5. Sekce Hodnot: 3 bloky (🌾 Český len · 🪡 Strojová výšivka · 🎁 Krásné balení)
6. Banner 3+1: zvýrazněný (hnědé pozadí), text o akci pro utěrky
7. Kontakt: tlačítko na Instagram @misiksvet
8. Patička: copyright + text o 14 dnech vrácení + GDPR poznámka

**Vizuální kontroly — mobil (DevTools: Ctrl+Shift+M, 390×844):**

9. Hamburger tlačítko viditelné vpravo nahoře (navigační odkazy skryty)
10. Kliknutím na hamburger: menu se otevře (Příběh · Výrobky · Kontakt jako sloupcový seznam)
11. Kliknutím na odkaz v menu: menu se zavře, stránka scrolluje na sekci
12. Produktové karty: 1 sloupec na mobilu, 2 sloupce na tabletu
13. Sekce Příběh: text a foto pod sebou (1 sloupec)
14. Hodnoty: 1 sloupec na mobilu

**Fonty a barvy:**
15. Nadpisy: Cormorant Garamond (serif, elegantní) — viditelně jiný font od textu
16. Text: Josefin Sans (sans-serif, čisté)
17. Barvy: béžové/krémové pozadí, teplé hnědé nadpisy a akcenty, žádné křiklavé barvy

**Scrollování:**
18. Po scrollu dolu: navigační lišta získá jemné pozadí a stín (scrolled třída)
  </how-to-verify>
  <verify>
    <automated>MISSING — Toto je vizualni checkpoint, automatizovane overeni neni mozne. Uzivatelka musi potvrdit vizualne.</automated>
  </verify>
  <done>Uzivatelka napsala "approved" po vizualnim overeni vsech 18 kontrolnich bodu</done>
  <resume-signal>
Napiste "approved" pokud stranka vypada spravne, nebo popiste ktere sekce/elementy je potreba opravit.

Priklad "approved" odpovedi: "approved"
Priklad zpetne vazby: "Hero foto se nezobrazuje" nebo "Hamburger menu se neotvira" nebo "Produktove karty nemaji fotky"
  </resume-signal>
</task>

</tasks>

<verification>
Checkpoint je blokovaci — Phase 2 nezacina dokud Misa nenapise "approved".

Po schvaleni: vsechny Phase 1 pozadavky (DISP-01 az DISP-05, NAV-01 az NAV-04, LEGAL-01, LEGAL-02) jsou vizualne potvrzeny.
</verification>

<success_criteria>
- Misa potvrdila "approved" po vizualnim overeni
- Vsechny sekce jsou vizualne spravne a odpovidaji zadani
- Navigace funguje na mobilu (hamburger)
- Produktove karty zobrazuji vsechny 4 produkty s fotkami, popisy a cenami
- Legalni texty jsou v paticce
- Stranka je responzivni
</success_criteria>

<output>
Po dokonceni checkpointu vytvorit `.planning/phases/01-zaklad-a-zobrazeni-produktu/01-03-SUMMARY.md`
</output>
