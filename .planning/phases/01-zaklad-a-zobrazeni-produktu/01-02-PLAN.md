---
phase: 01-zaklad-a-zobrazeni-produktu
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - js/products.js
  - js/main.js
  - .nojekyll
autonomous: true
requirements:
  - DISP-03
  - NAV-02
  - NAV-04

must_haves:
  truths:
    - "Navstevnik vidi 4 produktove karty (kopretina, vlci mak, siska, zastera) vyrendrovane v mrizce"
    - "Kazda karta ma fotografii, nazev, popis, material a cenu"
    - "Hamburger menu se na mobilu otevira a zavira klikem"
    - "Navigacni liska zmeni pozadi (scrolled trida) po scrollu dolu"
    - "Hero sekce ma fade-in animaci pri nacteni"
    - ".nojekyll soubor existuje v koreni"
  artifacts:
    - path: "js/products.js"
      provides: "PRODUCTS pole — single source of truth pro produktova data"
      exports: ["PRODUCTS"]
      contains: "const PRODUCTS"
    - path: "js/main.js"
      provides: "renderProductCards(), initNav() — DOMContentLoaded entry point"
      contains: "DOMContentLoaded"
    - path: ".nojekyll"
      provides: "Prazdny soubor deaktivujici Jekyll na GitHub Pages"
  key_links:
    - from: "js/main.js"
      to: "js/products.js"
      via: "PRODUCTS array pouzity v renderProductCards()"
      pattern: "PRODUCTS\\.map"
    - from: "js/main.js"
      to: "index.html #product-grid"
      via: "document.getElementById('product-grid')"
      pattern: "getElementById\\('product-grid'\\)"
    - from: "js/main.js"
      to: "nav element"
      via: "document.querySelector('nav').classList.toggle('scrolled')"
      pattern: "classList.toggle\\('scrolled'\\)"
---

<objective>
Vytvori dve JS datove/logicke vrstvy a .nojekyll.

Purpose: Plan 01 pripravil HTML kostru s prazdnym #product-grid div a CSS styly vcetne .product-card stylu. Tento plan JS tuto kostru ozivi: products.js definuje produktova data, main.js vyrenderuje karty a inicializuje navigaci (hamburger + scroll shadow). Fade-in animace je cista CSS (z Plan 01) — Plan 02 ji nevyzaduje zadny JS.

Output: js/products.js (PRODUCTS pole), js/main.js (render + nav init), .nojekyll (prazdny soubor).
</objective>

<execution_context>
@C:/Users/misah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/misah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-zaklad-a-zobrazeni-produktu/01-CONTEXT.md
@.planning/phases/01-zaklad-a-zobrazeni-produktu/01-RESEARCH.md
@.planning/phases/01-zaklad-a-zobrazeni-produktu/01-01-SUMMARY.md

<!-- Interfaces created by Plan 01 that this plan depends on -->
<interfaces>
Z index.html (Plan 01):
  - `<div id="product-grid" class="product-grid"></div>` — prazdny container pro JS render
  - `<nav>` element — target pro classList toggle
  - `<button class="hamburger" aria-expanded="false">` — hamburger button
  - `<ul class="nav-links">` — nav links list

Z css/style.css (Plan 01):
  - `.product-card` — CSS trida pro kliknutelnou kartu (hover efekt, flex layout)
  - `.product-card__body`, `.product-card__name`, `.product-card__desc`, `.product-card__material`, `.product-card__price` — CSS tridy pro obsah karty
  - `nav.scrolled` — CSS trida aplikovana JS po scrollu
  - `.nav-links.open` — CSS trida otevirajici hamburger menu na mobilu

Script load order v index.html:
  1. js/products.js (musi byt nacteny pred main.js — main.js pouziva PRODUCTS globalni promennou)
  2. js/main.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vytvorit js/products.js — PRODUCTS pole s daty vsech 4 produktu</name>
  <files>js/products.js</files>
  <action>
Vytvorit adresar `js/` a soubor `js/products.js`. Soubor obsahuje POUZE data — zadny DOM pristup, zadne funkce krome konstanty.

```javascript
// js/products.js
// Single source of truth pro produktova data.
// Phase 2 prida property "type" pro ucel akce 3+1 (uterky vs zastera).
// Phase 2 prida tlacitko "Pridat do kosiku" — zatim pouze vizualni karta.

const PRODUCTS = [
  {
    id: 'kopretina',
    name: 'Utěrka Kopretina',
    price: 380,
    image: 'fotky/20251208_185910.jpg',
    description: 'Luční kopretiny na přírodním lnu. Jemný motiv ve třech barvách.',
    material: '100% český len'
  },
  {
    id: 'vlci-mak',
    name: 'Utěrka Vlčí mák',
    price: 380,
    image: 'fotky/20251208_190226.jpg',
    description: 'Rudý květ vlčího máku s listy. Výrazný a elegantní.',
    material: '100% český len'
  },
  {
    id: 'siska',
    name: 'Utěrka Šiška s větvičkou',
    price: 380,
    image: 'fotky/20251208_190438.jpg',
    description: 'Detailní šiška s jehličím. Ideální dárek pro milovníky přírody.',
    material: '100% český len'
  },
  {
    id: 'zastera',
    name: 'Zástěra',
    price: 890,
    image: 'fotky/20251115_111917.jpg',
    description: 'Lněná zástěra s kopretinou a humorem. \u201eNemám šajnu co z toho bude.\u201c',
    material: '100% český len'
  }
];
```

Poznamky:
- Cesty k fotkam jsou relativni: `fotky/nazev.jpg` — NIKDY `/fotky/...`
- Ceske uvozovky pro zastery popis: `\u201e` = „ (dolni), `\u201c` = " (horni)
- Soubor NEMA `export` statement — pouzivame prostou globalni promennou (zadny bundler, zadne ES modules)
- Komentare o Phase 2 rozsirenich jsou soucast souboru — pomahaji budoucim planum pochopit kontextu
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); eval(fs.readFileSync('C:/Users/misah/Documents/Claude_code/web_misiksvet/js/products.js','utf8')); const checks=[['PRODUCTS is array',Array.isArray(PRODUCTS)],['4 products',PRODUCTS.length===4],['kopretina',PRODUCTS.some(p=>p.id==='kopretina')],['vlci-mak',PRODUCTS.some(p=>p.id==='vlci-mak')],['siska',PRODUCTS.some(p=>p.id==='siska')],['zastera',PRODUCTS.some(p=>p.id==='zastera')],['kopretina price 380',PRODUCTS.find(p=>p.id==='kopretina').price===380],['zastera price 890',PRODUCTS.find(p=>p.id==='zastera').price===890],['image paths relative',PRODUCTS.every(p=>p.image.startsWith('fotky/') && !p.image.startsWith('/'))],['all have material',PRODUCTS.every(p=>p.material && p.material.length > 0)]]; checks.forEach(([name,ok])=>console.log((ok?'OK':'FAIL')+' '+name)); process.exit(checks.every(([,ok])=>ok)?0:1)"</automated>
  </verify>
  <done>js/products.js obsahuje const PRODUCTS se 4 produkty: kopretina (380 Kc), vlci-mak (380 Kc), siska (380 Kc), zastera (890 Kc); vsechny maji id, name, price, image, description, material; cesty k fotkam jsou relativni</done>
</task>

<task type="auto">
  <name>Task 2: Vytvorit js/main.js — renderovani karet + inicializace navigace + .nojekyll</name>
  <files>js/main.js, .nojekyll</files>
  <action>
Vytvorit `js/main.js` s nasledujicim kodem. Soubor musi bezet az po products.js (zajisteno poradim scriptu v index.html z Plan 01).

```javascript
// js/main.js
// Phase 1: renderovani produktovych karet + inicializace navigace
// Phase 2 prida: inicializaci kosiku (cart.js)
// Phase 3 prida: inicializaci formulare (order.js)

/**
 * Vyrenderuje produktove karty do #product-grid z PRODUCTS pole (products.js).
 * Phase 1: pouze vizualni karta — zadne tlacitko "Pridat do kosiku".
 * Phase 2 prida: <button class="btn-add" data-id="${p.id}">Pridat do kosiku</button>
 */
function renderProductCards() {
  const grid = document.getElementById('product-grid');
  if (!grid) return;

  grid.innerHTML = PRODUCTS.map(p => `
    <article class="product-card">
      <img
        src="${p.image}"
        alt="${p.name} — ${p.material}"
        loading="lazy"
        width="600"
        height="600"
      >
      <div class="product-card__body">
        <h3 class="product-card__name">${p.name}</h3>
        <p class="product-card__desc">${p.description}</p>
        <p class="product-card__material">${p.material}</p>
        <p class="product-card__price">${p.price}&nbsp;Kč</p>
      </div>
    </article>
  `).join('');
}

/**
 * Inicializuje navigaci:
 * - Hamburger toggle (klik otevira/zavira .nav-links pomoci tridy .open)
 * - Scroll shadow (.scrolled trida na nav po scrollu > 10px)
 */
function initNav() {
  const nav = document.querySelector('nav');
  const hamburger = document.querySelector('.hamburger');
  const navLinks = document.querySelector('.nav-links');

  if (hamburger && navLinks) {
    hamburger.addEventListener('click', () => {
      navLinks.classList.toggle('open');
      hamburger.setAttribute(
        'aria-expanded',
        navLinks.classList.contains('open') ? 'true' : 'false'
      );
    });

    // Zavreni menu po kliknuti na odkaz (UX na mobilu)
    navLinks.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        navLinks.classList.remove('open');
        hamburger.setAttribute('aria-expanded', 'false');
      });
    });
  }

  if (nav) {
    window.addEventListener('scroll', () => {
      nav.classList.toggle('scrolled', window.scrollY > 10);
    }, { passive: true });
  }
}

// Entry point
document.addEventListener('DOMContentLoaded', () => {
  renderProductCards();
  initNav();
});
```

Pote vytvorit `.nojekyll` v koreni repozitare:
- Prazdny soubor (nulova velikost)
- Deaktivuje Jekyll preprocessing na GitHub Pages
- MUSI byt v koreni repozitare (ne v podadresari)

Cesta: `C:/Users/misah/Documents/Claude_code/web_misiksvet/.nojekyll`

Poznamky pro main.js:
- `if (!grid) return` — obrana proti chybe pokud HTML nema #product-grid
- `if (hamburger && navLinks)` — obrana proti chybe pokud HTML nemá spravne elementy
- `{ passive: true }` na scroll listener — povinne pro vykon (eliminuje jank)
- Zavrit menu po kliknuti na navlink — nutne UX na mobilu (jinak menu zustane otevrene po kotvicim skoku)
- Fade-in animace je ciste CSS (@keyframes fadeIn v style.css) — main.js ji NESPUSTI ani neridi
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const main=fs.readFileSync('C:/Users/misah/Documents/Claude_code/web_misiksvet/js/main.js','utf8'); const nojekyll=fs.existsSync('C:/Users/misah/Documents/Claude_code/web_misiksvet/.nojekyll'); const checks=[['renderProductCards function',main.includes('function renderProductCards()')],['initNav function',main.includes('function initNav()')],['DOMContentLoaded',main.includes('DOMContentLoaded')],['getElementById product-grid',main.includes(\"getElementById('product-grid')\")],['PRODUCTS.map',main.includes('PRODUCTS.map')],['hamburger toggle',main.includes('.hamburger')],['nav-links open toggle',main.includes(\"classList.toggle('open')\")],['passive scroll listener',main.includes('passive: true')],['aria-expanded',main.includes('aria-expanded')],['close menu on link click',main.includes('classList.remove')],['.nojekyll exists',nojekyll]]; checks.forEach(([name,ok])=>console.log((ok?'OK':'FAIL')+' '+name)); process.exit(checks.every(([,ok])=>ok)?0:1)"</automated>
  </verify>
  <done>js/main.js obsahuje renderProductCards() ktera pouziva PRODUCTS.map, initNav() s hamburger toggle a scroll shadow, DOMContentLoaded entry point, pasivni scroll listener; .nojekyll existuje v koreni repozitare</done>
</task>

</tasks>

<verification>
Po dokonceni obou tasku spustit v prohlizeci (Live Server nebo double-click na index.html):

1. Produktova sekce — overit ze 4 karty jsou viditelne s fotkami, nazvy, popisy a cenami
2. Hamburger menu na mobilu (DevTools: 390px) — kliknutim se otevre/zavre navigace
3. Scrollovani — po scrollu dolu se liska ztemni (scrolled trida)
4. Navigacni kotvy (Pribeh, Vyrobky, Kontakt) — kliknutim plynule scrolluje na sekci, menu se zavre

Zkontrolovat konzoli prohlizece na JS chyby (F12 > Console — mela by byt prazdna).
</verification>

<success_criteria>
- js/products.js obsahuje PRODUCTS pole se 4 produkty (vsechna data spravna, ceny spravne, cesty relativni)
- js/main.js renderuje karty z PRODUCTS pole do #product-grid
- Hamburger toggle funguje (aria-expanded se meni, .open trida se pridava/odebira z .nav-links)
- Nav dostane .scrolled tridu po scrollu pomoci pasivniho scroll listeneru
- Navlink klik na mobilu zavre menu
- .nojekyll existuje v koreni repozitare jako prazdny soubor
</success_criteria>

<output>
Po dokonceni tasku vytvorit `.planning/phases/01-zaklad-a-zobrazeni-produktu/01-02-SUMMARY.md`
</output>
