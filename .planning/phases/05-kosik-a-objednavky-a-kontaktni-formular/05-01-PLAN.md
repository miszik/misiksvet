---
phase: 05-kosik-a-objednavky-a-kontaktni-formular
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/style.css
  - js/products.js
autonomous: true
requirements: [CART-01, CART-05, CART-06, CART-07, ORDER-01, ORDER-02, ORDER-03, ORDER-04, ORDER-05, ORDER-06, ORDER-07, ORDER-08, ORDER-09, ORDER-10, EMAIL-03]

must_haves:
  truths:
    - "index.html nacita EmailJS CDN a Packeta widget CDN scriptem v <head> pred ostatnimi scripty"
    - "Nav obsahuje tlacitko kosiku s SVG ikonou a <span class=\"cart-count\"> pro odznak"
    - "index.html obsahuje <div id=\"cart-drawer\"> a <div id=\"cart-overlay\"> hned za <nav>"
    - "Sekce #kontakt obsahuje <form id=\"order-form\" novalidate> s vsemi povinne poli (full_name, email, phone, delivery, gdpr) a podminenou sekcemi (packeta-section, home-delivery-section)"
    - "css/style.css obsahuje styly pro .cart-drawer (position: fixed, right 0, transform translateX(100%)) a .cart-drawer.open (translateX(0))"
    - "js/products.js ma type: 'uterka' pro kopretina/vlci-mak/siska a type: 'zastera' pro zastera"
  artifacts:
    - path: "index.html"
      provides: "HTML struktura pro kosik + formular + CDN scripty"
      contains: "cart-drawer, cart-overlay, order-form, packeta-section, home-delivery-section"
    - path: "css/style.css"
      provides: "Vizualni styly pro kosik, formular, stavove zpravy"
      contains: ".cart-drawer, .cart-overlay, .form-group, #form-success, #form-error"
    - path: "js/products.js"
      provides: "Produktova data s type polem pro promo logiku"
      contains: "type: 'uterka', type: 'zastera'"
  key_links:
    - from: "index.html <head>"
      to: "EmailJS CDN"
      via: "script src cdn.jsdelivr.net/@emailjs/browser@4"
      pattern: "emailjs/browser@4"
    - from: "index.html <body>"
      to: "Packeta widget CDN"
      via: "script src widget.packeta.com/v6"
      pattern: "packeta.com/v6"
    - from: "nav .nav-links"
      to: "#cart-drawer"
      via: "button#cart-nav-btn otevre drawer"
      pattern: "cart-nav-btn"
---

<objective>
HTML/CSS scaffold a data priprava pro interaktivni funkce Phase 5.

Purpose: Tato plan pripravuje vsechny HTML struktury a CSS styly, ktere cart.js (Plan 02) a order.js (Plan 03) potrebuji. Zadna JS logika se zde nepise — pouze HTML kontrakty a vizualni vrstvy. Executori Plan 02 a Plan 03 dostaji presne HTML selektor nazvy, ktere ocekavaji.

Output:
- index.html: CDN scripty, cart icon v nav, cart drawer, order form
- css/style.css: cart drawer, form styly, stavove zpravy
- js/products.js: pridano pole type pro promo logiku
</objective>

<execution_context>
@C:/Users/misah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/misah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-kosik-a-objednavky-a-kontaktni-formular/05-RESEARCH.md

<interfaces>
<!-- Existujici HTML struktura z Phase 1 (index.html) — klic pro spravne vlozeni noveho obsahu -->

Existujici <nav> struktura (riadek 15-27 index.html):
```html
<nav>
  <a href="#" class="nav-logo">Mišik svět</a>
  <button class="hamburger" aria-label="Otevřít menu" aria-expanded="false">
    <span class="hamburger__bar"></span>
    <span class="hamburger__bar"></span>
    <span class="hamburger__bar"></span>
  </button>
  <ul class="nav-links">
    <li><a href="#pribeh">Příběh</a></li>
    <li><a href="#produkty">Výrobky</a></li>
    <li><a href="#kontakt">Kontakt</a></li>
  </ul>
</nav>
```

Existujici sekce #kontakt (riadek 85-89 index.html):
```html
<section id="kontakt" class="section section--surface">
  <h2>Kontakt</h2>
  <p>Napište mi na Instagram – domluvíme motiv, počet kusů i termín.</p>
  <a href="https://www.instagram.com/misiksvet/" class="btn btn--primary" ...>@misiksvet na Instagramu</a>
</section>
```

Existujici script load order (riadky 97-98 index.html):
```html
<script src="js/products.js"></script>
<script src="js/main.js"></script>
```

Existujici PRODUCTS pole (js/products.js) — bez pole type:
```javascript
const PRODUCTS = [
  { id: 'kopretina', name: 'Utěrka Kopretina', price: 380, image: 'fotky/...', description: '...', material: '100% český len' },
  { id: 'vlci-mak',  name: 'Utěrka Vlčí mák', price: 380, image: 'fotky/...', description: '...', material: '100% český len' },
  { id: 'siska',     name: 'Utěrka Šiška s větvičkou', price: 380, image: 'fotky/...', description: '...', material: '100% český len' },
  { id: 'zastera',   name: 'Zástěra', price: 890, image: 'fotky/...', description: '...', material: '100% český len' }
];
```

Existujici CSS tokeny (css/style.css — pouzit pro konzistenci):
- var(--color-bg): pozadi stranky (#faf8f4)
- var(--color-text): zakladni text
- var(--color-accent): akcent/primarni barva
- var(--color-surface): povrch sekci
- var(--space-*): spacingove tokeny
- Existujici trid: .btn, .btn--primary, .btn--secondary, .section, .section--surface
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDN scripty, cart ikona v nav, cart drawer HTML, script load order</name>
  <files>index.html</files>
  <action>
Proved 4 zmeny v index.html:

**1. Do `<head>` pred `<link rel="stylesheet">` vloz EmailJS CDN a inicializaci:**
```html
<!-- EmailJS CDN — musi byt pred cart.js a order.js scripty -->
<script
  type="text/javascript"
  src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js">
</script>
<script type="text/javascript">
  (function(){
    emailjs.init({ publicKey: "YOUR_EMAILJS_PUBLIC_KEY" });
  })();
</script>
```
Placeholder `YOUR_EMAILJS_PUBLIC_KEY` ponechat tak — Misa doda klice pozdeji.

**2. Do `<ul class="nav-links">` pridej cart tlacitko jako posledni `<li>`:**
```html
<li>
  <button class="cart-nav-btn" aria-label="Otevřít košík" id="cart-nav-btn">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
      <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/>
      <line x1="3" y1="6" x2="21" y2="6"/>
      <path d="M16 10a4 4 0 01-8 0"/>
    </svg>
    <span class="cart-count" hidden>0</span>
  </button>
</li>
```

**3. Hned ZA uzaviraci `</nav>` vloz cart drawer a overlay:**
```html
<!-- Cart drawer — otevre se z prava pri kliknuti na ikonu kosiku -->
<div id="cart-drawer" class="cart-drawer" aria-hidden="true">
  <div class="cart-drawer__header">
    <h2>Košík</h2>
    <button class="cart-drawer__close" aria-label="Zavřít košík">✕</button>
  </div>
  <div id="cart-items" class="cart-drawer__items">
    <!-- Dynamicky plneno cart.js -->
  </div>
  <div class="cart-drawer__footer">
    <p class="cart-total"></p>
    <p class="cart-discount" hidden></p>
    <a href="#kontakt" class="btn btn--primary cart-checkout-btn">Přejít k objednávce</a>
  </div>
</div>
<div id="cart-overlay" class="cart-overlay"></div>
```

**4. Sekci #kontakt NAHRAD timto novym obsahem** (zachovej pouze `<section id="kontakt" class="section section--surface">`):
```html
<section id="kontakt" class="section section--surface">
  <h2>Objednat</h2>
  <p>Vyplňte formulář a objednávku odešlete. Platební instrukce vám pošleme e-mailem.</p>
  <p class="kontakt-instagram">Nebo nás najdete na <a href="https://www.instagram.com/misiksvet/" target="_blank" rel="noopener">Instagramu @misiksvet</a>.</p>

  <form id="order-form" novalidate>
    <div class="form-group">
      <label for="full_name">Jméno a příjmení *</label>
      <input type="text" id="full_name" name="full_name" required autocomplete="name">
    </div>
    <div class="form-group">
      <label for="email">E-mail *</label>
      <input type="email" id="email" name="email" required autocomplete="email">
    </div>
    <div class="form-group">
      <label for="phone">Telefon *</label>
      <input type="tel" id="phone" name="phone" required autocomplete="tel" placeholder="+420 123 456 789">
    </div>
    <div class="form-group">
      <label for="delivery">Způsob doručení *</label>
      <select id="delivery" name="delivery" required>
        <option value="">— Vyberte —</option>
        <option value="packeta-vydejna">Zásilkovna — výdejní místo</option>
        <option value="packeta-domu">Zásilkovna — doručení domů</option>
        <option value="osobni-odber">Osobní odběr (Ostrava)</option>
      </select>
    </div>

    <div id="packeta-section" hidden>
      <button type="button" id="packeta-open-btn" class="btn btn--secondary">
        Vybrat výdejní místo
      </button>
      <p id="packeta-selected-name" class="packeta-selected"></p>
      <p id="packeta-error" class="form-error" hidden></p>
    </div>

    <div id="home-delivery-section" hidden>
      <div class="form-group">
        <label for="home_address">Doručovací adresa *</label>
        <input type="text" id="home_address" name="home_address" autocomplete="street-address">
      </div>
    </div>

    <div class="form-group">
      <label for="note">Poznámka (nepovinné)</label>
      <textarea id="note" name="note" rows="3"></textarea>
    </div>

    <div class="form-group form-group--checkbox">
      <label>
        <input type="checkbox" name="gdpr" required>
        Souhlasím se zpracováním osobních údajů pro účely vyřízení objednávky. *
      </label>
    </div>

    <p id="cart-error" class="form-error" hidden></p>
    <button type="submit" id="submit-btn" class="btn btn--primary">Odeslat objednávku</button>
    <p id="form-success" class="form-success" hidden>Objednávka odeslána! Potvrzení dorazí na váš e-mail.</p>
    <p id="form-error" class="form-error" hidden>Něco se nepovedlo. Zkuste to prosím znovu nebo nás kontaktujte na Instagramu.</p>
  </form>
</section>
```

**5. Script load order na konci `<body>` — aktualizuj na:**
```html
<script src="https://widget.packeta.com/v6/www/js/library.js"></script>
<script src="js/products.js"></script>
<script src="js/cart.js"></script>
<script src="js/order.js"></script>
<script src="js/main.js"></script>
```
POZNAMKA: Packeta widget MUSI byt nacten SYNCHRONNE (bez defer/async) pred order.js. EmailJS je jiz v `<head>`. Packeta je v `<body>` pred ostatnimi scripty.

DULEZITE: js/cart.js a js/order.js zatim neexistuji — to je v poradku, budou vytvoreny v Planu 02 a 03. index.html je v tomto okamziku staticka struktura.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const checks=['emailjs/browser@4','YOUR_EMAILJS_PUBLIC_KEY','cart-nav-btn','cart-count','cart-drawer','cart-overlay','order-form','packeta-section','home-delivery-section','full_name','delivery','gdpr','submit-btn','form-success','form-error','packeta-error','cart-error','packeta.com/v6','js/cart.js','js/order.js'];const failed=checks.filter(c=>!h.includes(c));if(failed.length){console.error('FAILED:',failed);process.exit(1);}else{console.log('OK: vsechny kontroly prosly')}"</automated>
  </verify>
  <done>index.html obsahuje EmailJS CDN v head, cart tlacitko v nav, cart drawer po nav, kompletni order form v #kontakt, a spravny script load order s Packeta CDN a cart.js/order.js</done>
</task>

<task type="auto">
  <name>Task 2: CSS styly pro cart drawer, formular, stavove zpravy + type pole v products.js</name>
  <files>css/style.css, js/products.js</files>
  <action>
**css/style.css — PRIDEJ na konec souboru** (NEPREPIS existujici CSS):

```css
/* ============================================================
   PHASE 5: CART DRAWER
   ============================================================ */

.cart-nav-btn {
  background: none;
  border: none;
  cursor: pointer;
  color: var(--color-text);
  display: flex;
  align-items: center;
  gap: 0.25rem;
  padding: 0.25rem;
  position: relative;
}

.cart-count {
  background: var(--color-accent);
  color: #fff;
  font-size: 0.65rem;
  font-family: 'Josefin Sans', sans-serif;
  font-weight: 400;
  min-width: 1.25rem;
  height: 1.25rem;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  position: absolute;
  top: -0.3rem;
  right: -0.3rem;
}

.cart-drawer {
  position: fixed;
  top: 0;
  right: 0;
  width: min(400px, 100vw);
  height: 100vh;
  background: var(--color-bg);
  box-shadow: -4px 0 24px rgba(0, 0, 0, 0.15);
  transform: translateX(100%);
  transition: transform 0.3s ease;
  z-index: 200;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}

.cart-drawer.open {
  transform: translateX(0);
}

.cart-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 199;
  display: none;
}

.cart-overlay.visible {
  display: block;
}

.cart-drawer__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem;
  border-bottom: 1px solid rgba(0, 0, 0, 0.1);
}

.cart-drawer__header h2 {
  font-size: 1.25rem;
  margin: 0;
}

.cart-drawer__close {
  background: none;
  border: none;
  font-size: 1.25rem;
  cursor: pointer;
  color: var(--color-text);
  padding: 0.25rem 0.5rem;
}

.cart-drawer__items {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
}

.cart-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.75rem 0;
  border-bottom: 1px solid rgba(0, 0, 0, 0.06);
}

.cart-item__name {
  flex: 1;
  font-size: 0.9rem;
}

.cart-item__qty {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.cart-item__qty button {
  background: none;
  border: 1px solid rgba(0, 0, 0, 0.2);
  width: 1.75rem;
  height: 1.75rem;
  border-radius: 4px;
  cursor: pointer;
  font-size: 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
}

.cart-item__price {
  font-size: 0.9rem;
  min-width: 4rem;
  text-align: right;
}

.cart-item__remove {
  background: none;
  border: none;
  cursor: pointer;
  color: rgba(0, 0, 0, 0.4);
  font-size: 0.9rem;
  padding: 0.25rem;
}

.cart-empty {
  color: rgba(0, 0, 0, 0.45);
  font-size: 0.9rem;
  text-align: center;
  padding: 2rem 0;
}

.cart-drawer__footer {
  padding: 1rem 1.5rem 1.5rem;
  border-top: 1px solid rgba(0, 0, 0, 0.1);
}

.cart-total {
  font-weight: 600;
  margin-bottom: 0.25rem;
}

.cart-discount {
  color: #2a7a2a;
  font-size: 0.9rem;
  margin-bottom: 0.75rem;
}

.cart-checkout-btn {
  display: block;
  width: 100%;
  text-align: center;
  margin-top: 0.75rem;
}

/* ============================================================
   PHASE 5: ORDER FORM
   ============================================================ */

#order-form {
  max-width: 540px;
  margin: 2rem auto 0;
}

.form-group {
  margin-bottom: 1.25rem;
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
}

.form-group label {
  font-family: 'Josefin Sans', sans-serif;
  font-size: 0.85rem;
  font-weight: 400;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  color: var(--color-text);
}

.form-group input,
.form-group select,
.form-group textarea {
  padding: 0.65rem 0.85rem;
  border: 1px solid rgba(0, 0, 0, 0.2);
  border-radius: 4px;
  font-family: 'Josefin Sans', sans-serif;
  font-size: 0.95rem;
  background: var(--color-bg);
  color: var(--color-text);
  width: 100%;
  box-sizing: border-box;
  transition: border-color 0.15s;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--color-accent);
}

.form-group--checkbox {
  flex-direction: row;
  align-items: flex-start;
  gap: 0.6rem;
}

.form-group--checkbox label {
  text-transform: none;
  font-size: 0.85rem;
  letter-spacing: 0;
  line-height: 1.5;
  display: flex;
  align-items: flex-start;
  gap: 0.5rem;
  cursor: pointer;
}

.form-group--checkbox input[type="checkbox"] {
  width: auto;
  margin-top: 0.15rem;
  flex-shrink: 0;
}

#packeta-section,
#home-delivery-section {
  margin-bottom: 1.25rem;
}

.packeta-selected {
  font-size: 0.9rem;
  margin-top: 0.5rem;
  color: #2a7a2a;
}

.form-error {
  color: #c0392b;
  font-size: 0.85rem;
  margin-top: 0.25rem;
}

.form-success {
  color: #2a7a2a;
  font-size: 0.95rem;
  margin-top: 1rem;
  padding: 0.75rem 1rem;
  background: #f0faf0;
  border-radius: 4px;
}

#submit-btn {
  width: 100%;
  margin-top: 0.5rem;
}

#submit-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.kontakt-instagram {
  font-size: 0.9rem;
  color: rgba(0, 0, 0, 0.55);
  margin-bottom: 1.5rem;
}
```

**js/products.js — PRIDEJ `type` pole** ke kazdemu produktu:
- kopretina: `type: 'uterka'`
- vlci-mak: `type: 'uterka'`
- siska: `type: 'uterka'`
- zastera: `type: 'zastera'`

Toto je jedina zmena v products.js — pridej `type` property za property `material` pro kazdy objekt. Nic dalsiho nemen.

Vysledny PRODUCTS array musi vypadat takto:
```javascript
const PRODUCTS = [
  { id: 'kopretina', ..., material: '100% český len', type: 'uterka' },
  { id: 'vlci-mak',  ..., material: '100% český len', type: 'uterka' },
  { id: 'siska',     ..., material: '100% český len', type: 'uterka' },
  { id: 'zastera',   ..., material: '100% český len', type: 'zastera' }
];
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const css=fs.readFileSync('css/style.css','utf8');const js=fs.readFileSync('js/products.js','utf8');const cssChecks=['.cart-drawer','.cart-drawer.open','translateX(100%)','translateX(0)','cart-overlay','.form-group','.form-error','.form-success','#submit-btn:disabled'];const jsChecks=[\"type: 'uterka'\",\"type: 'zastera'\"];const failed=[...cssChecks.filter(c=>!css.includes(c)),...jsChecks.filter(c=>!js.includes(c))];if(failed.length){console.error('FAILED:',failed);process.exit(1);}else{console.log('OK: vsechny kontroly prosly')}"</automated>
  </verify>
  <done>css/style.css obsahuje vsechny cart a form styly na konci souboru. js/products.js ma type: 'uterka' u tri uterek a type: 'zastera' u zastery.</done>
</task>

</tasks>

<verification>
Po dokonceni obou tasku:
1. `node -e "const h=require('fs').readFileSync('index.html','utf8'); console.log('EmailJS:', h.includes('emailjs/browser@4')); console.log('Packeta CDN:', h.includes('packeta.com/v6')); console.log('cart.js:', h.includes('js/cart.js')); console.log('order.js:', h.includes('js/order.js')); console.log('cart-drawer:', h.includes('cart-drawer')); console.log('order-form:', h.includes('order-form'));"` — vsechny musi byt `true`
2. `node -e "const p=require('fs').readFileSync('js/products.js','utf8'); console.log('uterka count:', (p.match(/type: 'uterka'/g)||[]).length, '(ocekavano 3)'); console.log('zastera count:', (p.match(/type: 'zastera'/g)||[]).length, '(ocekavano 1)');"` — 3 uterky, 1 zastera
</verification>

<success_criteria>
- index.html obsahuje vsechny HTML kontrakty pro Plan 02 (cart.js) a Plan 03 (order.js)
- CDN scripty jsou nacitany ve spravnem poradi (EmailJS v head, Packeta sync v body pred cart.js/order.js)
- css/style.css obsahuje cart drawer a form CSS bez pretepsani existujicich stylu
- products.js ma type pole — promo logika v Planu 02 muze spravne filtrovat uterky vs zastera
- Zadna JS logika (cart, order) nebyla napsana — to je ulohou Planu 02 a 03
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/05-kosik-a-objednavky-a-kontaktni-formular/05-01-SUMMARY.md`
</output>
