---
phase: 05-kosik-a-objednavky-a-kontaktni-formular
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - js/order.js
  - js/main.js
autonomous: true
requirements: [ORDER-01, ORDER-02, ORDER-03, ORDER-04, ORDER-05, ORDER-06, ORDER-07, ORDER-08, ORDER-09, ORDER-10, EMAIL-01, EMAIL-02, EMAIL-03]

user_setup:
  - service: emailjs
    why: "Odesilani potvrzovacich emailu zakaznikovi a notifikaci Mise"
    env_vars:
      - name: EMAILJS_PUBLIC_KEY
        source: "EmailJS Dashboard -> Account -> Public Key"
      - name: EMAILJS_SERVICE_ID
        source: "EmailJS Dashboard -> Email Services -> [your service] -> Service ID"
      - name: EMAILJS_CUSTOMER_TEMPLATE_ID
        source: "EmailJS Dashboard -> Email Templates -> customer_confirmation -> Template ID"
      - name: EMAILJS_OWNER_TEMPLATE_ID
        source: "EmailJS Dashboard -> Email Templates -> owner_notification -> Template ID"
    dashboard_config:
      - task: "Pridat email sluzbu (Gmail nebo Seznam)"
        location: "EmailJS Dashboard -> Email Services -> Add New Service"
      - task: "Vytvorit sablonu 'customer_confirmation' pro zakaznika"
        location: "EmailJS Dashboard -> Email Templates -> Create New Template"
      - task: "Vytvorit sablonu 'owner_notification' pro Misu"
        location: "EmailJS Dashboard -> Email Templates -> Create New Template"
      - task: "Nastavit Allowed Origins na https://misiksvet.cz pred spustenim"
        location: "EmailJS Dashboard -> Account -> Security -> Allowed Origins"
  - service: packeta
    why: "Packeta/Zasilkovna widget pro vyber vydejna bodu na mape"
    env_vars:
      - name: PACKETA_API_KEY
        source: "client.packeta.com -> Settings -> API key (vyzaduje registraci Zasilkovna uctu)"
    dashboard_config:
      - task: "Registrovat ucet na client.packeta.com nebo kontaktovat info@zasilkovna.cz pro testovaci klic"
        location: "https://client.packeta.com"

must_haves:
  truths:
    - "Odeslani prazdneho formulare zobrazi ceske chybove hlasky na kazdem chybejicim poli — stranka se nenacita znovu (e.preventDefault je prvni radek handleru)"
    - "Zmena doruceni na 'packeta-vydejna' zobrazi sekci s tlacitkem pro otevreni Packeta widgetu"
    - "Zmena doruceni na 'packeta-domu' zobrazi pole pro dorucovacu adresu; prepnuti zpet tu hodnotu vycisti"
    - "Po uspesnem odeslani se tlacitko zobrazuje jako 'Odesíláno…' a neni znovu aktivovatelne"
    - "Prazdny kosik pri odeslani formulare zobrazi chybu 'Košík je prázdný'"
    - "Emailjs.send() je volano s rucne sestavenym params objektem (ne sendForm) — cart_summary je JS-computed string"
  artifacts:
    - path: "js/order.js"
      provides: "Formularova validace, Packeta widget integrace, EmailJS odeslani"
      exports: ["initOrderForm"]
    - path: "js/main.js"
      provides: "Entry point nyni vola initOrderForm()"
      contains: "initOrderForm()"
  key_links:
    - from: "js/order.js handleFormSubmit()"
      to: "js/cart.js loadCart() + calcTotal()"
      via: "prima volani globalnich funkci (loadCart, calcTotal, clearCart)"
      pattern: "loadCart\\(\\).*calcTotal\\("
    - from: "js/order.js handleFormSubmit()"
      to: "emailjs.send()"
      via: "await emailjs.send(SERVICE_ID, TEMPLATE_ID, params)"
      pattern: "emailjs\\.send"
    - from: "delivery select onChange"
      to: "#packeta-section a #home-delivery-section"
      via: "handleDeliveryChange() toggle hidden attribute"
      pattern: "handleDeliveryChange"
---

<objective>
Formularova validace, Packeta widget a EmailJS dvou-emailova integrace.

Purpose: order.js je posledni JS vrstva. Pouziva globalni funkce z cart.js (loadCart, calcTotal, clearCart) — ty musi existovat z Planu 02. Formulari implementuje ceske validacni hlasky, dynamicke doruceni sekce, Packeta widget callback a EmailJS.send() s rucne sestavenym params.

Output:
- js/order.js: validateForm, handleDeliveryChange, openPacketaWidget, handleFormSubmit, formatCartSummary, initOrderForm — vse jako plain globals
- js/main.js: pridano initOrderForm() volani na DOMContentLoaded
</objective>

<execution_context>
@C:/Users/misah/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/misah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-kosik-a-objednavky-a-kontaktni-formular/05-RESEARCH.md
@.planning/phases/05-kosik-a-objednavky-a-kontaktni-formular/05-02-SUMMARY.md

<interfaces>
<!-- Globalni funkce z cart.js (Plan 02) ktere order.js PRIMO VOLA — zadny import -->

Z js/cart.js (plain globals dostupne kdyz js/cart.js je nacten pred order.js):
```javascript
loadCart()     // → Array<{id: string, qty: number}>
calcTotal(cart) // → {gross: number, discount: number, net: number}
clearCart()    // → void — smaze localStorage a re-renderuje prazdny kosik
```

PRODUCTS konstanta (z products.js — nacten jako prvni v index.html):
```javascript
// Dostupna jako globalni PRODUCTS pole
// Kazda polozka ma: id, name, price, image, description, material, type
```

HTML selektory v index.html (vytvorene v Planu 01):
```
document.getElementById('order-form')         // <form id="order-form" novalidate>
document.getElementById('submit-btn')          // tlacitko odeslat
document.getElementById('packeta-section')     // hidden div pro Packeta
document.getElementById('home-delivery-section') // hidden div pro domaci adresu
document.getElementById('packeta-open-btn')    // tlacitko "Vybrat výdejní místo"
document.getElementById('packeta-selected-name') // <p> pro zobrazeni vybrane pobocky
document.getElementById('packeta-error')       // <p class="form-error" hidden>
document.getElementById('cart-error')          // <p class="form-error" hidden>
document.getElementById('form-success')        // <p class="form-success" hidden>
document.getElementById('form-error')          // <p class="form-error" hidden>

Form fields (name atributy):
  [name="full_name"]   required
  [name="email"]       required, type=email
  [name="phone"]       required, type=tel
  [name="delivery"]    required, select
  [name="home_address"] (v #home-delivery-section, ne required v HTML — validujeme JS podmineně)
  [name="note"]        textarea, ne required
  [name="gdpr"]        required, checkbox
```

Script load order v index.html (Plan 01):
```html
<script src="https://widget.packeta.com/v6/www/js/library.js"></script>  <!-- sync, az pak: -->
<script src="js/products.js"></script>
<script src="js/cart.js"></script>
<script src="js/order.js"></script>
<script src="js/main.js"></script>
```
Tedy: `Packeta` globalni objekt JE dostupny kdyz order.js se spousti.
`emailjs` globalni objekt JE dostupny (inicializovan v `<head>`).

Packeta Widget API:
```javascript
Packeta.Widget.pick(apiKey, callback, options)
// callback(point) kde point je objekt s: name, city, id
// point je null kdyz uzivatel zavre widget bez vyberu
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vytvorit js/order.js — validace, Packeta widget, EmailJS send</name>
  <files>js/order.js</files>
  <action>
Vytvor `js/order.js` jako NOVY soubor. Vsechny funkce jsou plain globals. Komentar v cestine.

```javascript
// js/order.js
// Objednavkovy formular: validace, Packeta widget, EmailJS send.
// Zavisi na: cart.js (loadCart, calcTotal, clearCart — musi byt nacten drive)
// EmailJS je inicializovan v <head> index.html.
// Packeta.Widget je dostupny pres synchronni CDN script v <body> pred timto souborem.

const PACKETA_API_KEY = 'YOUR_PACKETA_API_KEY'; // Doplni Misa z client.packeta.com
const EMAILJS_SERVICE_ID = 'YOUR_SERVICE_ID';   // Doplni Misa z EmailJS dashboard
const EMAILJS_CUSTOMER_TEMPLATE = 'YOUR_CUSTOMER_TEMPLATE_ID'; // "customer_confirmation"
const EMAILJS_OWNER_TEMPLATE = 'YOUR_OWNER_TEMPLATE_ID';       // "owner_notification"

let selectedPickupPoint = null; // Ulozi vybrany Packeta bod

// ── Packeta widget ────────────────────────────────────────────────────────────

function openPacketaWidget() {
  const options = {
    language: 'cs',
    country: 'cz',
    view: 'modal',
    valueFormat: '"Packeta",id,name,city,street',
    vendors: [{ country: 'cz' }]
  };

  Packeta.Widget.pick(PACKETA_API_KEY, function(point) {
    if (point) {
      selectedPickupPoint = point;
      const nameEl = document.getElementById('packeta-selected-name');
      if (nameEl) nameEl.textContent = point.name + ', ' + point.city;
      const errEl = document.getElementById('packeta-error');
      if (errEl) errEl.hidden = true;
    }
  }, options);
}

// ── Delivery select handler ───────────────────────────────────────────────────

function handleDeliveryChange(value) {
  const packetaSection = document.getElementById('packeta-section');
  const homeSection = document.getElementById('home-delivery-section');
  const homeAddressInput = document.querySelector('[name="home_address"]');

  if (packetaSection) packetaSection.hidden = value !== 'packeta-vydejna';
  if (homeSection) homeSection.hidden = value !== 'packeta-domu';

  // Vycisti hodnoty skrytych poli — jinak by se dostaly do emailu
  if (value !== 'packeta-vydejna') {
    selectedPickupPoint = null;
    const nameEl = document.getElementById('packeta-selected-name');
    if (nameEl) nameEl.textContent = '';
  }
  if (value !== 'packeta-domu' && homeAddressInput) {
    homeAddressInput.value = '';
  }
}

// ── Form validation ──────────────────────────────────────────────────────────
// Pouzivame novalidate na formu + manualni validaci pro ceske hlasky.
// setCustomValidity() + reportValidity() zobrazi browser bublinu s ceskou zpravou.

function showFieldError(elId, message) {
  const el = document.getElementById(elId);
  if (el) {
    el.textContent = message;
    el.hidden = false;
  }
}

function hideFieldError(elId) {
  const el = document.getElementById(elId);
  if (el) el.hidden = true;
}

function validateForm() {
  let valid = true;

  // Standardni povinne fieldy s Czech hlasky
  const fieldValidations = [
    {
      el: document.querySelector('[name="full_name"]'),
      msg: 'Vyplňte prosím jméno a příjmení.'
    },
    {
      el: document.querySelector('[name="email"]'),
      msg: 'Zadejte platnou e-mailovou adresu.'
    },
    {
      el: document.querySelector('[name="phone"]'),
      msg: 'Zadejte telefonní číslo (např. +420 123 456 789).'
    },
    {
      el: document.querySelector('[name="delivery"]'),
      msg: 'Vyberte způsob doručení.'
    },
    {
      el: document.querySelector('[name="gdpr"]'),
      msg: 'Pro odeslání objednávky je nutný souhlas se zpracováním osobních údajů.'
    }
  ];

  fieldValidations.forEach(({ el, msg }) => {
    if (!el) return;
    el.setCustomValidity('');
    if (!el.checkValidity()) {
      el.setCustomValidity(msg);
      el.reportValidity();
      if (valid) el.focus(); // fokus na prvni chybne pole
      valid = false;
    }
  });

  // Podmínena validace: home_address kdyz delivery = packeta-domu
  const deliveryVal = document.querySelector('[name="delivery"]')?.value;
  if (deliveryVal === 'packeta-domu') {
    const homeInput = document.querySelector('[name="home_address"]');
    if (homeInput && !homeInput.value.trim()) {
      homeInput.setCustomValidity('Vyplňte doručovací adresu.');
      homeInput.reportValidity();
      valid = false;
    } else if (homeInput) {
      homeInput.setCustomValidity('');
    }
  }

  // Packeta bod: vybran kdyz delivery = packeta-vydejna
  if (deliveryVal === 'packeta-vydejna' && !selectedPickupPoint) {
    showFieldError('packeta-error', 'Vyberte prosím výdejní místo Zásilkovny.');
    valid = false;
  } else {
    hideFieldError('packeta-error');
  }

  // Prazdny kosik
  const cart = loadCart();
  if (cart.length === 0) {
    showFieldError('cart-error', 'Košík je prázdný. Přidejte prosím produkty před odesláním.');
    valid = false;
  } else {
    hideFieldError('cart-error');
  }

  return valid;
}

// ── Cart summary formatter ────────────────────────────────────────────────────
// Generuje textovy prehled kosiku pro emailjs params.

function formatCartSummary(cart) {
  const lines = cart.map(item => {
    const product = PRODUCTS.find(p => p.id === item.id);
    if (!product) return '';
    return product.name + ' x' + item.qty + ' = ' + (product.price * item.qty) + ' Kč';
  }).filter(Boolean);

  const { gross, discount, net } = calcTotal(cart);
  if (discount > 0) {
    lines.push('Sleva akce 3+1: −' + discount + ' Kč');
  }
  lines.push('CELKEM: ' + net + ' Kč');
  return lines.join('\n');
}

// ── Form submit handler ───────────────────────────────────────────────────────

async function handleFormSubmit(e) {
  e.preventDefault(); // MUSI byt prvni — zabranni reload stranky na GitHub Pages

  if (!validateForm()) return;

  const submitBtn = document.getElementById('submit-btn');
  if (submitBtn) {
    submitBtn.disabled = true;
    submitBtn.textContent = 'Odesílám…';
  }

  // Schovej predchozi stavove zpravy
  const successEl = document.getElementById('form-success');
  const errorEl = document.getElementById('form-error');
  if (successEl) successEl.hidden = true;
  if (errorEl) errorEl.hidden = true;

  const cart = loadCart();
  const { net } = calcTotal(cart);
  const cartSummary = formatCartSummary(cart);

  const deliveryEl = document.querySelector('[name="delivery"]');
  const deliveryLabels = {
    'packeta-vydejna': 'Zásilkovna — výdejní místo',
    'packeta-domu': 'Zásilkovna — doručení domů',
    'osobni-odber': 'Osobní odběr (Ostrava)'
  };
  const deliveryLabel = deliveryLabels[deliveryEl?.value] || deliveryEl?.value || '';

  const params = {
    full_name:    document.querySelector('[name="full_name"]')?.value || '',
    email:        document.querySelector('[name="email"]')?.value || '',
    phone:        document.querySelector('[name="phone"]')?.value || '',
    delivery:     deliveryLabel,
    pickup_point: selectedPickupPoint
      ? selectedPickupPoint.name + ', ' + selectedPickupPoint.city
      : '—',
    home_address: document.querySelector('[name="home_address"]')?.value || '—',
    note:         document.querySelector('[name="note"]')?.value || '—',
    cart_summary: cartSummary,
    total:        net + ' Kč'
  };

  try {
    // 1. Notifikace Mise
    await emailjs.send(EMAILJS_SERVICE_ID, EMAILJS_OWNER_TEMPLATE, params);
    // 2. Potvrzeni zakaznikovi
    await emailjs.send(EMAILJS_SERVICE_ID, EMAILJS_CUSTOMER_TEMPLATE, params);

    // Uspech
    if (successEl) successEl.hidden = false;
    if (submitBtn) {
      submitBtn.textContent = 'Odesláno ✓';
      // Tlacitko zusatva deaktivovano po uspesnem odeslani — zabranna duplicit
    }

    clearCart(); // Smaze localStorage a re-renderuje prazdny kosik

  } catch (err) {
    console.error('EmailJS error:', err);
    if (errorEl) errorEl.hidden = false;
    // Re-aktivuj tlacitko pouze pri chybe — uzivatel muze zkusit znovu
    if (submitBtn) {
      submitBtn.disabled = false;
      submitBtn.textContent = 'Odeslat objednávku';
    }
  }
}

// ── Init ─────────────────────────────────────────────────────────────────────

function initOrderForm() {
  const form = document.getElementById('order-form');
  if (!form) return;

  form.addEventListener('submit', handleFormSubmit);

  const deliverySelect = document.querySelector('[name="delivery"]');
  if (deliverySelect) {
    deliverySelect.addEventListener('change', (e) => handleDeliveryChange(e.target.value));
  }

  const packetaBtn = document.getElementById('packeta-open-btn');
  if (packetaBtn) {
    packetaBtn.addEventListener('click', openPacketaWidget);
  }
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const js=fs.readFileSync('js/order.js','utf8');const checks=['function openPacketaWidget()','function handleDeliveryChange(','function validateForm()','function formatCartSummary(','function handleFormSubmit(','function initOrderForm()','e.preventDefault()','submitBtn.disabled = true','clearCart()','loadCart()','calcTotal(','emailjs.send(','EMAILJS_SERVICE_ID','EMAILJS_OWNER_TEMPLATE','EMAILJS_CUSTOMER_TEMPLATE','YOUR_PACKETA_API_KEY','YOUR_SERVICE_ID','packeta-vydejna','packeta-domu','Košík je prázdný'];const failed=checks.filter(c=>!js.includes(c));if(failed.length){console.error('FAILED:',failed);process.exit(1);}else{console.log('OK: order.js ma vsechny pozadovane funkce')}"</automated>
  </verify>
  <done>js/order.js existuje se vsemi funkcemi jako plain globals. handleFormSubmit vola e.preventDefault() jako prvni, deaktivuje tlacitko pred send, vola emailjs.send() dvakrat (owner + customer), vola clearCart() po uspechu. validateForm kontroluje prazdny kosik a Packeta vyber.</done>
</task>

<task type="auto">
  <name>Task 2: Aktualizovat js/main.js — pridat initOrderForm() volani</name>
  <files>js/main.js</files>
  <action>
V `js/main.js` najdi DOMContentLoaded entry point a pridej `initOrderForm()` volani.

Vysledny entry point musi vypadat presne takto:

```javascript
document.addEventListener('DOMContentLoaded', () => {
  renderProductCards();
  initNav();
  initCart();
  initOrderForm();
});
```

DULEZITE: Poradi volani je dulezite:
1. `renderProductCards()` — vytvori .btn-add tlacitka ktere initCart() bude zachytavat
2. `initNav()` — navigacni hamburger a scroll shadow
3. `initCart()` — delegovany listener na product-grid, nacte ulozeny kosik
4. `initOrderForm()` — pripouje submit listener na order-form

Odeber komentar `// initOrderForm(); — bude pridano v Planu 03` pokud existuje z Planu 02.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const js=fs.readFileSync('js/main.js','utf8');const checks=['initOrderForm()','initCart()','renderProductCards()','initNav()'];const failed=checks.filter(c=>!js.includes(c));if(failed.length){console.error('FAILED:',failed);process.exit(1);}else{console.log('OK: main.js vola vsechny 4 init funkce')}"</automated>
  </verify>
  <done>js/main.js DOMContentLoaded vola renderProductCards(), initNav(), initCart() a initOrderForm() v tomto poradi.</done>
</task>

</tasks>

<verification>
Kompletni Phase 5 funkcni test (vyzaduje prohlizec — provedeno v Planu 04 checkpointu):
1. Otevre index.html — zadne JS chyby v konzoli (krome "YOUR_PACKETA_API_KEY" a "YOUR_SERVICE_ID" — to jsou ocekavane placeholdery)
2. Formulari submit s prazdnymi poli — zobrazi se ceske validacni hlasky
3. Zmena doruceni na "Zásilkovna — výdejní místo" — zobrazi #packeta-section
4. Zmena zpet na "Osobní odběr" — scryje #packeta-section, vycisti selectedPickupPoint
5. Zmena na "Zásilkovna — doručení domů" — zobrazi #home-delivery-section
6. Automate check: `node -e "['js/cart.js','js/order.js','js/main.js'].forEach(f=>console.log(f,require('fs').existsSync(f)?'OK':'MISSING'))"` — vsechny OK
</verification>

<success_criteria>
- js/order.js existuje se vsemi 6 funkcemi jako plain globals
- handleFormSubmit: e.preventDefault() prvni, submitBtn.disabled ihned, emailjs.send() pres params objekt (ne sendForm), clearCart() po uspechu
- validateForm: ceske hlasky, Packeta validace, prazdny kosik validace, home_address podminenou validace
- handleDeliveryChange: cisteni hodnot skrytych poli pri prepnuti
- main.js DOMContentLoaded vola vsechny 4 init funkce ve spravnem poradi
- Placeholdery YOUR_PACKETA_API_KEY, YOUR_SERVICE_ID, YOUR_CUSTOMER_TEMPLATE_ID, YOUR_OWNER_TEMPLATE_ID v order.js — Misa doplni po nastaveni uctu
</success_criteria>

<output>
Po dokonceni vytvor `.planning/phases/05-kosik-a-objednavky-a-kontaktni-formular/05-03-SUMMARY.md`
</output>
